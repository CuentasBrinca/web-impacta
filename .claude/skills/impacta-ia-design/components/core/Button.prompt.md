One-sentence: Pill-shaped call-to-action button in Impacta IA's four brand variants (magenta primary, outline secondary, ghost, WhatsApp green).

```jsx
<Button variant="primary" size="lg" arrow>Quiero participar</Button>
<Button variant="secondary">Ser sponsor</Button>
<Button as="a" href="#form" variant="ghost">Ver programa</Button>
```

Variants: `primary` (magenta `#ed1e79`), `secondary` (transparent w/ white-alpha border, brightens on hover), `ghost`, `whatsapp`.
Sizes: `sm` | `md` | `lg`. Set `arrow` to append the → glyph the site uses on nearly every CTA. Uses FH Lecturis Rounded Bold and a full pill radius. Press-scales to 0.97.
